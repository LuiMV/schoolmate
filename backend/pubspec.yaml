name: schoolmate_backend
description: SchoolMate backend server with JWT authentication
publish_to: none

environment:
  sdk: ^3.11.1

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  jose: ^0.3.0
  dotenv: ^4.2.0
  crypto: ^3.0.3
  mongo_dart: ^0.10.0
  bcrypt: ^1.1.3
